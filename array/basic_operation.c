#include<stdio.h>

main()
{
    int arr[];
    while(True)
    {
        int ck;
        printf("Choose the various option in the following:");
        printf("\n1. Insertion\n2. Deletion\n3.Exit");
        scanf("%d",&ck);
        if(ck==1)
        {
            Insertion();
        }
        else if(ck==2)
        {
            Deletion();
        }
        else if(ck==)
    }

}